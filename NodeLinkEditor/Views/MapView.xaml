<UserControl x:Class="NodeLinkEditor.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NodeLinkEditor.Views"
             xmlns:vm="clr-namespace:NodeLinkEditor.ViewModels"
             mc:Ignorable="d"
              d:DesignHeight="450" d:DesignWidth="800"
             x:Name="RootControl"
             xmlns:converter="clr-namespace:NodeLinkEditor.Converters">
    <UserControl.DataContext>
        <vm:MapEditorViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <converter:AdjNodePositionConverter x:Key="AdjNodePositionConverter"/>
        <converter:BoolToColorConverter x:Key="BoolToColorConverter"/>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                   CanContentScroll="True">
        <Grid>
            <Canvas Width="{Binding MapData.Width}" Height="{Binding MapData.Height}" Background="{DynamicResource {x:Static SystemColors.AccentColorBrushKey}}"
                    MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                    MouseMove="Canvas_MouseMove"
                    MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
                    MouseLeave="Canvas_MouseLeave" >
                <Image Source="{Binding MapData.MapImage}" Width="{Binding MapData.Height}" Height="{Binding MapData.Height}" Stretch="Fill"/>
                <ItemsControl ItemsSource="{Binding Links}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Line X1="{Binding StartNode.X}" Y1="{Binding StartNode.Y}"
                                  X2="{Binding EndNode.X}"   Y2="{Binding EndNode.Y}"
                                  Stroke="Black" StrokeThickness="2"
                                  Tag="{Binding DataContext, ElementName=RootControl}">
                                <Line.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="削除"
                                                  Command="{Binding PlacementTarget.Tag.RemoveLinkCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding}"/>
                                    </ContextMenu>
                                </Line.ContextMenu>
                            </Line>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ItemsControl ItemsSource="{Binding Nodes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Ellipse Width="{Binding DataContext.NodeSize, ElementName=RootControl}"
                                     Height="{Binding DataContext.NodeSize, ElementName=RootControl}"
                                     Fill="{Binding IsSelected, Converter={StaticResource BoolToColorConverter}}"
                                     Tag="{Binding DataContext, ElementName=RootControl}">
                                <Ellipse.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="削除"
                                                  Command="{Binding PlacementTarget.Tag.RemoveNodeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding}"/>
                                    </ContextMenu>
                                </Ellipse.ContextMenu>
                            </Ellipse>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="Canvas.Left" Value="{Binding X,Converter={StaticResource AdjNodePositionConverter}}"/>
                            <Setter Property="Canvas.Top" Value="{Binding Y,Converter={StaticResource AdjNodePositionConverter}}"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </Canvas>
        </Grid>
    </ScrollViewer>
</UserControl>
